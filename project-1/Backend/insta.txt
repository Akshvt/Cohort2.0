
! FEATURES !


===> Authentication

  ==> Register (1.User ka data save krna 2. Token user ko dena)
  ==> Login
  ==> logout (Token Blacklisting)
  ==> [OTP based Registration] --> At the end

===> Post

  ==> Create
  ==> Can see the feed
  ==> Like posts (Collection Types)
  ==> Save Posts

===> Users

  ==> Followers
  ==> Following



  Imagekit --> image cloud storage


  user = {
    username:String,
    email:String,
    password:String,
    bio:String,
    followers:Array,
    profile_image:String
}

 post = {
  caption:String,
  img_url:Stringuser:userId,
  createdAt: Date-time,
 }


packages installed:

express
mongoose
dotenv
jwtwebtoken
cookie-parser
crypto(preinstalled)
express.authRouter

bcrypt
multer


In multer: 
2 types of storages :
 1. DiskStorage (we never store data in server bc very costly)
 2. Memory Storage (Temportary storage on server --> AWS ---> server deletes file, its now on AWS)